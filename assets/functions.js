function goBack() {
	window.history.back();
}

jQuery(document).ready(function() {
	
	// Toggle button
	/*document.querySelector('.c-hamburger').addEventListener('click', function() {
		mmenu.toggle();
	});
	*/
/*	 var API = $("#menu").data( "mmenu" );

      $("c-hamburger").click(function() {
         API.open();
      });
*	
});

// Reset checked nav toggle on page reload
/*jQuery(window).load(function() {
	jQuery('html').addClass('loaded');
	jQuery('#mobileNavToggle').prop('checked', false);
});*/

});